# secrets — never commit
.env

# local submission data (LGPD: stays on the VPS, not in git)
data/
*.jsonl

# design reference images (third-party stock — kept off git)
refs/

# python
__pycache__/
*.pyc
